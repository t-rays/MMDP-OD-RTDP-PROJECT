# Convenience pointer for Colab and quick setups.
# The canonical dependency list lives in pyproject.toml; prefer:
#   pip install -e .[notebooks]
psutil>=5.9
matplotlib>=3.7
pandas>=2.0
numpy>=1.24
ipywidgets>=8.0
