# Test suite

- `unit/`: focused tests for configuration and reusable components.
- `integration/`: planner smoke tests and the subprocess/CSV end-to-end test.
- `conftest.py`: shared fixtures used by both groups.
